# generate-and-decode-QRCode-using-Java
This is a Java Program tha uses Google's Zxing(Zebra Crossing) library to generate QR code as an image file.
Also it decodes the QR code and outputs the text in it.
